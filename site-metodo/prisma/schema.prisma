generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

// 👤 Modelo de usuário atualizado com sistema de roles unificado
model User {
  id            String    @id @default(cuid())
  name          String?
  email         String?   @unique
  emailVerified DateTime?
  image         String?
  password      String?
  
  // 🔐 Sistema de autorização unificado
  accessLevel   Int       @default(1)          // Compatibilidade: mantém sistema legado
  roleType      UserRoleType @default(USER)    // Novo: sistema de roles moderno
  isActive      Boolean   @default(true)
  
  // 📊 Metadados e auditoria
  lastLogin     DateTime?
  loginCount    Int       @default(0)         // Contador de logins
  failedLogins  Int       @default(0)         // Tentativas de login falharam
  lastFailedLogin DateTime?                   // Último login com falha
  createdAt     DateTime  @default(now())
  updatedAt     DateTime  @updatedAt
  
  // 🔗 Relacionamentos Auth.js v5
  accounts      Account[]
  sessions      Session[]
  authenticators Authenticator[]
  
  // 🔐 Sistema ABAC - Relacionamentos
  userRoles     UserRole[]
  
  // 🔐 TOTP/MFA (preservado do sistema existente)
  totpSecret    String?   @map("totp_secret")
  mfaEnabled    Boolean   @default(false)     // Flag MFA ativo
  
  // 📝 Auditoria e logs
  auditLogs     AuditLog[]
  
  @@index([email])
  @@index([roleType])
  @@index([isActive])
  @@index([accessLevel])
}

// 🎭 Enum de tipos de roles do sistema (substitui accessLevel gradualmente)
enum UserRoleType {
  GUEST       // Visitante (accessLevel 0)
  USER        // Usuário padrão (accessLevel 1-49)
  MODERATOR   // Moderador (accessLevel 50-99)
  ADMIN       // Administrador (accessLevel 100+)
}

// 🔑 Authenticator para WebAuthn/Passkeys
model Authenticator {
  credentialID         String   @id
  userId               String
  providerAccountId    String
  credentialPublicKey  String
  counter              Int
  credentialDeviceType String
  credentialBackedUp   Boolean
  transports           String?
  user                 User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([userId, credentialID])
  @@index([providerAccountId])
}

// 🔗 Contas OAuth vinculadas (Auth.js v5)
model Account {
  id                String  @id @default(cuid())
  userId            String
  type              String
  provider          String
  providerAccountId String
  refresh_token     String?
  access_token      String?
  expires_at        Int?
  token_type        String?
  scope             String?
  id_token          String?
  session_state     String?
  user              User    @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([provider, providerAccountId])
  @@index([userId])
  @@index([provider])
}

// 📊 Sessões de usuário (Database Sessions)
model Session {
  id           String   @id @default(cuid())
  sessionToken String   @unique
  userId       String
  expires      DateTime
  user         User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  
  @@index([userId])
  @@index([expires])
}

// 🔐 Tokens de verificação (email, reset password, etc.)
model VerificationToken {
  identifier String
  token      String   @unique
  expires    DateTime

  @@unique([identifier, token])
  @@index([expires])
}

// 📋 Sistema de auditoria e logs de segurança
model AuditLog {
  id        String   @id @default(cuid())
  userId    String?
  action    AuditAction
  target    String?
  details   String?  // JSON com detalhes da ação
  ipAddress String?  // IP do usuário
  userAgent String?  // User agent do browser
  success   Boolean  @default(true)
  createdAt DateTime @default(now())
  
  user      User?    @relation(fields: [userId], references: [id], onDelete: SetNull)
  
  @@index([userId])
  @@index([action])
  @@index([createdAt])
  @@index([success])
}

// 🎯 Enum de ações de auditoria
enum AuditAction {
  // Autenticação
  LOGIN_SUCCESS
  LOGIN_FAILED
  LOGOUT
  SIGNUP
  PASSWORD_CHANGE
  EMAIL_CHANGE
  
  // OAuth
  OAUTH_LINK
  OAUTH_UNLINK
  
  // MFA/TOTP
  MFA_ENABLE
  MFA_DISABLE
  TOTP_VERIFY
  
  // Administração
  USER_CREATE
  USER_UPDATE
  USER_DELETE
  USER_ACTIVATE
  USER_DEACTIVATE
  ROLE_CHANGE
  
  // Sistema
  SESSION_CREATE
  SESSION_DELETE
  ACCOUNT_LINK
  ACCOUNT_UNLINK
}

// 🔐 Sistema ABAC/Casbin - Roles
model Role {
  id          String @id @default(cuid())
  name        String @unique
  description String?
  attributes  String? // JSON com atributos do role
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
  
  // Relacionamentos
  userRoles   UserRole[]
  
  @@index([name])
}

// 🔗 Relacionamento usuário-role (many-to-many)
model UserRole {
  id     String @id @default(cuid())
  userId String
  roleId String
  
  // Metadados
  assignedAt DateTime @default(now())
  assignedBy String?  // ID do usuário que atribuiu o role
  expiresAt  DateTime? // Role temporário
  isActive   Boolean @default(true)
  
  // Relacionamentos
  user User @relation(fields: [userId], references: [id], onDelete: Cascade)
  role Role @relation(fields: [roleId], references: [id], onDelete: Cascade)
  
  @@unique([userId, roleId])
  @@index([userId])
  @@index([roleId])
  @@index([isActive])
}

// 🛡️ Políticas de autorização ABAC
model AuthorizationPolicy {
  id         String @id @default(cuid())
  subject    String // Usuário, role ou atributo
  object     String // Recurso ou padrão de recurso
  action     String // Ação permitida
  effect     PolicyEffect @default(allow)
  conditions String? // Condições ABAC em formato JSON
  description String?
  isActive   Boolean @default(true)
  
  // Metadados
  createdAt  DateTime @default(now())
  updatedAt  DateTime @updatedAt
  createdBy  String? // ID do usuário que criou a política
  
  @@index([subject])
  @@index([object])
  @@index([action])
  @@index([effect])
  @@index([isActive])
}

// 📊 Enum para efeito das políticas
enum PolicyEffect {
  allow
  deny
}

// 📈 Logs de acesso e autorização
model AccessLog {
  id         String @id @default(cuid())
  userId     String?
  resource   String
  action     String
  result     AccessResult
  reason     String? // Motivo da decisão
  policies   String? // JSON com políticas aplicadas
  context    String? // JSON com contexto da requisição
  
  // Metadados de request
  ipAddress  String?
  userAgent  String?
  timestamp  DateTime @default(now())
  
  @@index([userId])
  @@index([resource])
  @@index([action])
  @@index([result])
  @@index([timestamp])
}

// 🎯 Enum para resultado de acesso
enum AccessResult {
  allow
  deny
}
