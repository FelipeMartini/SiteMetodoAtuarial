// Adiciona os tipos do jest-dom para o TypeScript reconhecer os matchers personalizados
import '@testing-library/jest-dom'

// Comentário: Este arquivo garante que o TypeScript reconheça os matchers como 'toBeInTheDocument' em todos os testes.
