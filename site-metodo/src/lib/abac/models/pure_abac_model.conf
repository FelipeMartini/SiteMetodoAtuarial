# Pure ABAC Model for Site Método Atuarial
# 100% Attribute-Based Access Control - No roles or hierarchies

[request_definition]
r = sub, obj, act, ctx

[policy_definition]
p = sub_rule, obj_rule, act, ctx_rule, eft

[policy_effect]
e = some(where (p.eft == allow))

[matchers]
# Pure ABAC evaluation using subject, object, action and context attributes
m = eval(p.sub_rule) && eval(p.obj_rule) && r.act == p.act && eval(p.ctx_rule)
