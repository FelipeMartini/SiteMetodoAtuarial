// Arquivo gerado automaticamente pelo processo de limpeza.
// Placeholder simples para evitar rota vazia. Substituir pela implementação real quando disponível.

export default function PageRecuperacao() {
	return (
		<div className="p-8">
			<h1 className="text-2xl font-semibold">Recuperação de senha</h1>
			<p className="mt-4 text-muted-foreground">Página em desenvolvimento — implementação pendente.</p>
		</div>
	);
}

