'use client'
// Página de gerenciamento de provedores (account linking) no perfil do usuário
// UI/UX moderna, dark/light mode, integração Auth.js v5 + Prisma

import React from 'react'
import PainelAccountLinking from './PainelAccountLinking'

export default function PageAccountLinking() {
  return <PainelAccountLinking />
}
